# iOS zero-duration keyboard frame

Applies zero-duration iOS keyboard frame changes synchronously so Compose insets follow externally sampled presentation frames without an additional display-link cycle.
