diff --git a/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/platform/UIKitTextInputService.ios.kt b/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/platform/UIKitTextInputService.ios.kt
index 19e6f16594d065da6284b78f4d762f10f9f73920..e99423c8702286eef7277435f78f7641c50000f4 100644
--- a/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/platform/UIKitTextInputService.ios.kt
+++ b/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/platform/UIKitTextInputService.ios.kt
@@ -24,6 +24,7 @@ import androidx.compose.ui.ExperimentalComposeUiApi
 import androidx.compose.ui.geometry.Offset
 import androidx.compose.ui.geometry.Rect
 import androidx.compose.ui.graphics.Color
+import androidx.compose.ui.input.key.Key
 import androidx.compose.ui.input.key.KeyEvent
 import androidx.compose.ui.scene.ComposeSceneFocusManager
 import androidx.compose.ui.text.TextLayoutResult
@@ -148,8 +149,12 @@ internal class UIKitTextInputService(
     fun beginHardwareKeyboardRepeat(event: KeyEvent): Boolean =
         currentInputConnection?.beginHardwareKeyboardRepeat(event) == true
 
-    fun endHardwareKeyboardRepeat(): Boolean =
-        currentInputConnection?.endHardwareKeyboardRepeat() == true
+    fun onHardwareKeyboardEvent(event: KeyEvent) {
+        currentInputConnection?.onHardwareKeyboardEvent(event)
+    }
+
+    fun endHardwareKeyboardRepeat(key: Key): Boolean =
+        currentInputConnection?.endHardwareKeyboardRepeat(key) == true
 
     fun resetHardwareKeyboardRepeat() {
         currentInputConnection?.resetHardwareKeyboardRepeat()
diff --git a/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/scene/ComposeSceneMediator.ios.kt b/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/scene/ComposeSceneMediator.ios.kt
index 41566f0f76f1d09d8db5f635e29773399853571d..f08063a117a88844d5a1234ceff225448a5c6d2a 100644
--- a/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/scene/ComposeSceneMediator.ios.kt
+++ b/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/scene/ComposeSceneMediator.ios.kt
@@ -37,7 +37,9 @@ import androidx.compose.ui.input.key.Key
 import androidx.compose.ui.input.key.KeyEvent
 import androidx.compose.ui.input.key.KeyEventType
 import androidx.compose.ui.input.key.PointerKeyboardModifiers
+import androidx.compose.ui.input.key.copy
 import androidx.compose.ui.input.key.internal
+import androidx.compose.ui.input.key.key
 import androidx.compose.ui.input.key.toComposeEvent
 import androidx.compose.ui.input.key.type
 import androidx.compose.ui.input.pointer.HistoricalChange
@@ -117,6 +119,7 @@ import platform.UIKit.UIEvent
 import platform.UIKit.UIEventButtonMaskPrimary
 import platform.UIKit.UIEventButtonMaskSecondary
 import platform.UIKit.UIPress
+import platform.UIKit.UIPressesEvent
 import platform.UIKit.UITouch
 import platform.UIKit.UITouchPhase
 import platform.UIKit.UITouchTypeDirect
@@ -798,10 +801,19 @@ internal class ComposeSceneMediator(
 
     private val routedHardwareKeyboardPresses = mutableListOf<RoutedHardwareKeyboardPress>()
 
-    private fun onKeyboardPresses(presses: Set<*>): Set<*> =
+    private fun onKeyboardPresses(presses: Set<*>, nativeEvent: UIPressesEvent?): Set<*> =
         presses.filterTo(mutableSetOf()) { value ->
             val press = value as UIPress
-            val event = press.toComposeEvent()
+            val event =
+                press.toComposeEvent().let { event ->
+                    // Read modifiers from this event, not the key captured when the press began.
+                    if (nativeEvent == null) event
+                    else
+                        event.copy(
+                            modifiers = PointerKeyboardModifiers(nativeEvent.modifierFlagsOrZero)
+                        )
+                }
+            textInputService.onHardwareKeyboardEvent(event)
             when (event.type) {
                 KeyEventType.KeyDown -> routeHardwareKeyboardPressBegan(press, event).forwardsToUIKit
                 KeyEventType.KeyUp -> {
@@ -861,7 +873,7 @@ internal class ComposeSceneMediator(
     private fun finishHardwareKeyboardPress(press: UIPress) {
         val routedPress = routedHardwareKeyboardPresses.firstOrNull { it.press.isEqual(press) }
         if (routedPress?.route == HardwareKeyboardPressRoute.ApplicationWithNativeRepeat) {
-            textInputService.endHardwareKeyboardRepeat()
+            textInputService.endHardwareKeyboardRepeat(press.toComposeEvent().key)
         }
         routedHardwareKeyboardPresses.removeAll { it.press.isEqual(press) }
     }
diff --git a/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/text/input/HardwareKeyboardRepeatBridge.ios.kt b/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/text/input/HardwareKeyboardRepeatBridge.ios.kt
index a2ef2a9db2d6b180fa1e26f2e2281aeba989f475..01a9e6f8ccfa3defd02284aea0b4cfa6779659de 100644
--- a/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/text/input/HardwareKeyboardRepeatBridge.ios.kt
+++ b/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/text/input/HardwareKeyboardRepeatBridge.ios.kt
@@ -19,6 +19,8 @@ package androidx.compose.ui.text.input
 import androidx.compose.ui.input.key.Key
 import androidx.compose.ui.input.key.KeyEvent
 import androidx.compose.ui.input.key.KeyEventType
+import androidx.compose.ui.input.key.copy
+import androidx.compose.ui.input.key.internal
 import androidx.compose.ui.input.key.isAltPressed
 import androidx.compose.ui.input.key.isCtrlPressed
 import androidx.compose.ui.input.key.isMetaPressed
@@ -41,8 +43,20 @@ internal class HardwareKeyboardRepeatBridge(
         return true
     }
 
-    fun end(): Boolean {
-        if (activePress == null) return false
+    fun onKeyEvent(event: KeyEvent) {
+        if (event.type == KeyEventType.KeyDown && !event.key.isModifier()) {
+            // Selection/delete callbacks have no key identity. Once another editing key
+            // takes over, they must not be replayed as the previous press.
+            activePress = null
+        } else {
+            activePress?.let { active ->
+                active.event = active.event.copy(modifiers = event.internal.modifiers)
+            }
+        }
+    }
+
+    fun end(key: Key): Boolean {
+        if (activePress?.event?.key != key) return false
         activePress = null
         return true
     }
@@ -86,7 +100,7 @@ internal class HardwareKeyboardRepeatBridge(
 }
 
 private data class ActiveHardwareKeyboardPress(
-    val event: KeyEvent,
+    var event: KeyEvent,
     val source: NativeRepeatSource,
     var initialEchoConsumed: Boolean = false,
 )
@@ -96,6 +110,22 @@ private enum class NativeRepeatSource {
     DeleteBackward,
 }
 
+private fun Key.isModifier(): Boolean =
+    when (this) {
+        Key.ShiftLeft,
+        Key.ShiftRight,
+        Key.CtrlLeft,
+        Key.CtrlRight,
+        Key.AltLeft,
+        Key.AltRight,
+        Key.MetaLeft,
+        Key.MetaRight,
+        Key.CapsLock,
+        Key.NumLock,
+        Key.Function -> true
+        else -> false
+    }
+
 private fun KeyEvent.nativeRepeatSource(): NativeRepeatSource? {
     if (isCtrlPressed || isAltPressed || isMetaPressed) return null
     return when (key) {
diff --git a/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/text/input/TextInputConnection.ios.kt b/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/text/input/TextInputConnection.ios.kt
index 1e2abfbcc375c7dc6962cd6e8f2cbdf7024f3e1f..14961258810043a63181f488696e91ba2a5de31d 100644
--- a/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/text/input/TextInputConnection.ios.kt
+++ b/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/text/input/TextInputConnection.ios.kt
@@ -161,7 +161,11 @@ internal abstract class TextInputConnection(
     fun beginHardwareKeyboardRepeat(event: KeyEvent): Boolean =
         hardwareKeyboardRepeatBridge.begin(event, hasActiveComposition)
 
-    fun endHardwareKeyboardRepeat(): Boolean = hardwareKeyboardRepeatBridge.end()
+    fun onHardwareKeyboardEvent(event: KeyEvent) {
+        hardwareKeyboardRepeatBridge.onKeyEvent(event)
+    }
+
+    fun endHardwareKeyboardRepeat(key: Key): Boolean = hardwareKeyboardRepeatBridge.end(key)
 
     fun resetHardwareKeyboardRepeat() {
         hardwareKeyboardRepeatBridge.reset()
diff --git a/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/window/InputViews.ios.kt b/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/window/InputViews.ios.kt
index 52df9f5a9719eee3c445f69f9afd76f19967f423..19397d500e65321fcfa157a23ee05319f89d230b 100644
--- a/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/window/InputViews.ios.kt
+++ b/compose/ui/ui/src/iosMain/kotlin/androidx/compose/ui/window/InputViews.ios.kt
@@ -555,7 +555,7 @@ internal class OverlayInputView(
     onScrollEvent: (position: DpOffset, delta: DpOffset, event: UIEvent?, eventKind: TouchesEventKind) -> Unit,
     onCancelScroll: () -> Unit,
     private var onHoverEvent: (position: DpOffset, event: UIEvent?, eventKind: TouchesEventKind) -> Unit,
-    private var onKeyboardPresses: (Set<*>) -> Set<*>,
+    private var onKeyboardPresses: (Set<*>, UIPressesEvent?) -> Set<*>,
     private var onKeyboardPressesFinished: (Set<*>) -> Unit,
     isHigherPriorityGestureTrackingTouches: () -> Boolean,
     private var onRemoveSubview: () -> Unit,
@@ -635,7 +635,7 @@ internal class OverlayInputView(
     override fun canBecomeFocused(): Boolean = false
 
     override fun pressesBegan(presses: Set<*>, withEvent: UIPressesEvent?) {
-        val nativePresses = onKeyboardPresses(presses)
+        val nativePresses = onKeyboardPresses(presses, withEvent)
         if (nativePresses.isNotEmpty()) {
             super.pressesBegan(nativePresses, withEvent)
         }
@@ -643,7 +643,7 @@ internal class OverlayInputView(
     }
 
     override fun pressesEnded(presses: Set<*>, withEvent: UIPressesEvent?) {
-        val nativePresses = onKeyboardPresses(presses)
+        val nativePresses = onKeyboardPresses(presses, withEvent)
         if (nativePresses.isNotEmpty()) {
             super.pressesEnded(nativePresses, withEvent)
         }
@@ -651,7 +651,7 @@ internal class OverlayInputView(
     }
 
     override fun pressesCancelled(presses: Set<*>, withEvent: UIPressesEvent?) {
-        val nativePresses = onKeyboardPresses(presses)
+        val nativePresses = onKeyboardPresses(presses, withEvent)
         if (nativePresses.isNotEmpty()) {
             super.pressesCancelled(nativePresses, withEvent)
         }
@@ -780,7 +780,7 @@ internal class OverlayInputView(
         hitTestInteropView = { null }
         isPointInsideInteractionBounds = { false }
         canIgnoreDragGesture = { false }
-        onKeyboardPresses = { emptySet<Any>() }
+        onKeyboardPresses = { _, _ -> emptySet<Any>() }
         onKeyboardPressesFinished = {}
         onOutsidePointerEvent = {}
         onTouchesEvent = { _, _, _ -> PointerEventResult() }
diff --git a/compose/ui/ui/src/iosTest/kotlin/androidx/compose/ui/text/input/HardwareKeyboardRepeatBridgeTest.kt b/compose/ui/ui/src/iosTest/kotlin/androidx/compose/ui/text/input/HardwareKeyboardRepeatBridgeTest.kt
index e814cd7db85cff450c7708ebd31ca71a98cc5084..2a0b4ee8f24582c9470099fb8cbdaf2cb2c2c1d3 100644
--- a/compose/ui/ui/src/iosTest/kotlin/androidx/compose/ui/text/input/HardwareKeyboardRepeatBridgeTest.kt
+++ b/compose/ui/ui/src/iosTest/kotlin/androidx/compose/ui/text/input/HardwareKeyboardRepeatBridgeTest.kt
@@ -19,6 +19,8 @@ package androidx.compose.ui.text.input
 import androidx.compose.ui.input.key.Key
 import androidx.compose.ui.input.key.KeyEvent
 import androidx.compose.ui.input.key.KeyEventType
+import androidx.compose.ui.input.key.isShiftPressed
+import androidx.compose.ui.input.key.key
 import kotlin.test.Test
 import kotlin.test.assertEquals
 import kotlin.test.assertFalse
@@ -86,7 +88,63 @@ class HardwareKeyboardRepeatBridgeTest {
     }
 
     @Test
-    fun secondEditingPressCannotStealAnonymousNativeTicks() {
+    fun changingShiftWhileHoldingAnArrowUpdatesSubsequentRepeats() {
+        for (initialShift in listOf(false, true)) {
+            val repeats = mutableListOf<KeyEvent>()
+            val bridge = HardwareKeyboardRepeatBridge(repeats::add)
+            val down = keyDown(Key.DirectionDown, isShiftPressed = initialShift)
+            assertTrue(bridge.begin(down, hasActiveComposition = false))
+            assertTrue(bridge.onSelectionChange())
+
+            bridge.onKeyEvent(
+                KeyEvent(
+                    key = Key.ShiftLeft,
+                    type = if (initialShift) KeyEventType.KeyUp else KeyEventType.KeyDown,
+                    isShiftPressed = !initialShift,
+                )
+            )
+
+            assertTrue(bridge.onSelectionChange())
+            assertEquals(Key.DirectionDown, repeats.single().key)
+            assertEquals(!initialShift, repeats.single().isShiftPressed)
+        }
+    }
+
+    @Test
+    fun anotherNativeEditingKeyReleasesSelectionAndDeleteCallbacks() {
+        for (first in listOf(Key.DirectionDown, Key.Delete)) {
+            val repeats = mutableListOf<KeyEvent>()
+            val bridge = HardwareKeyboardRepeatBridge(repeats::add)
+            assertTrue(bridge.begin(keyDown(first), hasActiveComposition = false))
+
+            // The mediator observes every physical key before routing it to app or UIKit.
+            bridge.onKeyEvent(keyDown(Key.DirectionLeft))
+
+            assertFalse(bridge.onSelectionChange())
+            assertFalse(bridge.onDeleteBackward())
+            assertEquals(emptyList(), repeats)
+        }
+    }
+
+    @Test
+    fun releasingPreviousArrowDoesNotEndTheNewArrowsRepeat() {
+        val repeats = mutableListOf<KeyEvent>()
+        val bridge = HardwareKeyboardRepeatBridge(repeats::add)
+        assertTrue(bridge.begin(keyDown(Key.DirectionDown), hasActiveComposition = false))
+        val up = keyDown(Key.DirectionUp)
+        bridge.onKeyEvent(up)
+        assertTrue(bridge.begin(up, hasActiveComposition = false))
+        assertTrue(bridge.onSelectionChange())
+
+        assertFalse(bridge.end(Key.DirectionDown))
+        assertTrue(bridge.onSelectionChange())
+        assertEquals(listOf(up), repeats)
+        assertTrue(bridge.end(Key.DirectionUp))
+        assertFalse(bridge.onSelectionChange())
+    }
+
+    @Test
+    fun armingWithoutAnotherPhysicalKeyEventDoesNotReplaceActiveRepeat() {
         val repeats = mutableListOf<KeyEvent>()
         val bridge = HardwareKeyboardRepeatBridge(repeats::add)
         val down = keyDown(Key.DirectionDown)
@@ -104,7 +162,7 @@ class HardwareKeyboardRepeatBridgeTest {
         val event = keyDown(Key.DirectionDown)
 
         assertTrue(bridge.begin(event, hasActiveComposition = false))
-        bridge.end()
+        bridge.end(event.key)
         assertFalse(bridge.onSelectionChange())
 
         assertTrue(bridge.begin(event, hasActiveComposition = false))
diff --git a/compose/ui/ui/src/iosTest/kotlin/androidx/compose/ui/text/input/TextInputConnectionIosTest.kt b/compose/ui/ui/src/iosTest/kotlin/androidx/compose/ui/text/input/TextInputConnectionIosTest.kt
index a1c010887edd6a14584d01644d1b92f0bf812b8a..f3a149a7f0c8b1fe5e34f8aac6117b72c76512ca 100644
--- a/compose/ui/ui/src/iosTest/kotlin/androidx/compose/ui/text/input/TextInputConnectionIosTest.kt
+++ b/compose/ui/ui/src/iosTest/kotlin/androidx/compose/ui/text/input/TextInputConnectionIosTest.kt
@@ -49,6 +49,29 @@ import platform.UIKit.UIView
 
 @OptIn(ExperimentalForeignApi::class, ExperimentalCoroutinesApi::class, InternalComposeUiApi::class)
 class TextInputConnectionIosTest {
+    @Test
+    fun nativeSelectionAfterAnotherKeyIsNotConsumedAsVerticalRepeat() = runTest {
+        val request = TestRequest(TextFieldValue("abcd", TextRange(3)))
+        val connection = TestTextInputConnection(CoroutineScope(coroutineContext))
+        connection.start(request)
+        assertTrue(
+            connection.beginHardwareKeyboardRepeat(
+                KeyEvent(Key.DirectionDown, KeyEventType.KeyDown)
+            )
+        )
+        connection.setSelectedTextRange(TextRange(4)) // Initial native echo is consumed.
+
+        connection.onHardwareKeyboardEvent(KeyEvent(Key.DirectionLeft, KeyEventType.KeyDown))
+        connection.setSelectedTextRange(TextRange(2))
+        runCurrent()
+
+        assertEquals(TextFieldValue("abcd", TextRange(2)), request.value())
+        assertEquals(
+            listOf(listOf<EditCommand>(SetSelectionCommand(2, 2))),
+            request.dispatchedCommands,
+        )
+    }
+
     @Test
     fun nativeActionFlushAppliesSelectionBeforeTheNextEditAndDoesNotReplayIt() = runTest {
         val request = TestRequest(TextFieldValue("ab", TextRange(2)))
